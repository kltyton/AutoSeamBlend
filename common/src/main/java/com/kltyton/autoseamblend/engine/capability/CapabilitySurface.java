package com.kltyton.autoseamblend.engine.capability;

/** 中文：必须使用同一已解析方法计划的四个产品能力面。 / English: The four product surfaces that must consume the same resolved method plan. */
public enum CapabilitySurface {
    RUNTIME,
    PREVIEW,
    PNG_MATERIALIZE,
    BAKED_EXPORT
}
