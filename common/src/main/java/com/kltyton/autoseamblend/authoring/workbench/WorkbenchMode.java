package com.kltyton.autoseamblend.authoring.workbench;

/** 中文：可视化工作室一次只显示的顶层任务。 / English: Top-level visual studio task shown one at a time. */
public enum WorkbenchMode {
    TARGET_LIBRARY,
    CONNECTION_PREVIEW,
    TEXTURE_PAINT,
    NATIVE_PROPERTIES
}
